#include <stdio.h>
/**
 * main - This program prints a line with the printf function
 * description: this program prints a line with the printf function
 * return:0 (success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
